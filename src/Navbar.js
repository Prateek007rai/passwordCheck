const Navbar = () => {

    return(
        <div className="Navbar">
            <h2> ippo pay Assessment</h2>
            <p>Check password is strong or not , if not then show the steps to make it strong.</p>
        </div>
    )

}

export default Navbar;